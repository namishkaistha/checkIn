/**
 * Global ambient declarations for compile-time constants injected by Vite's
 * `define` option (see vite.config.ts). These let TypeScript treat the
 * substituted values as plain string literals at type-check time.
 */
declare const __APP_VERSION__: string;
